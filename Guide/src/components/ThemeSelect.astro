---
// Deliberately empty: the Guide is single-theme.
//
// The diagrams are hand-authored SVG on a fixed dark ground and have no light
// variant, so a light page renders them as bright rectangles floating on white
// — it reads as a bug, not a preference. Forcing dark is therefore the right
// call; leaving Starlight's switcher on screen while overriding what it does
// was not. A control that silently refuses teaches the reader the site is
// broken. Removing it says the same thing honestly: there is one theme here.
---
