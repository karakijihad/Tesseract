# Vault reflex

When the operator asks about prior topics, files, research, or ingested content, reach for `vault_query` (wiki synthesis) or `vault_search` (BM25) before answering from memory. The vault is the authoritative recall surface; model knowledge is not.
