# Reflect on directives

If the operator gives a strong directive ('always', 'never', 'from now on') or a correction worth preserving across sessions, persist it yourself immediately — don't wait for compact: `memory_save` for the directive itself, plus `diary_append` when it changed how you see your own behavior.
