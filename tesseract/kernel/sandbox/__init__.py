"""Sandbox primitives.

``_ipc_frames`` — the length-prefixed framing codec shared with
``orchestrator.agent_controller`` — is imported directly from its
submodule by callers. There is no provisional-tool sandbox here.
"""
