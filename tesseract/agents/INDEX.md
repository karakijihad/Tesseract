# TESSERACT Agent Registry

Markdown-defined sub-agents loaded at runtime by `tesseract/agents/loader.py`.
Each row MUST correspond to a `.md` file in this directory. Ghost entries (no backing file) are not allowed.

Each `model_role` cell accepts a role name from `tesseract/config/roles.yaml` (current roster: `chat_brain`, `claude_cli`, `codex_cli`, `observer_agent`, `agents_default`, `subagents_default`, `vision_agent`, `image_generator`, `audio_transcribe`, `feedback_consolidator`, `autonomy_heartbeat`, `autonomy_strategist`) OR a provider-model reference of shape `<tier>.<provider>.<model_id>` (e.g. `api.openai.gpt54_nano`) from `tesseract/config/providers.yaml`. CLI-suffixed roles + `cli.<provider>.<model>` refs are rejected by `invoke_agent` — invoke those agents via `delegate_coder` / `delegate_auditor` instead.

| name                  | model_role          | description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| vault-librarian       | agents_default      | Vault wiki compiler and query synthesizer. Processes raw vault sources into interlinked wiki pages, maintains INDEX.md, and synthesizes answers from vault knowledge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| multi-session-planner | claude_cli          | Structural planner for initiatives >3 sessions. Produces folder-based, dependency-linked, audit-gated plan (INDEX / GOVERNANCE / SESSION-RITUAL / \_shared/ / phase files / audits/). Invoke explicitly with a project brief via `delegate_coder`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| observer              | observer_agent      | Stateful peripheral observer. Watches chat history, PTY output (with consent), and memory deltas; emits `memory_suggestion` envelopes that the assistant consumes at turn boundary. Never writes memory directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| memory-classifier     | chat_brain          | Single-turn section classifier. Routes unprefixed daily-file sections into user / feedback / project / reference. Returns `{type, confidence}` JSON only — no tool use, no history.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| vault-lint            | agents_default      | Vault contradiction detector. Reads two Source-page summaries that share concepts and classifies their relationship as `weaken` / `qualify` / `contradict` (jdbranham four-verb scheme; `reinforce` dropped by the tool). JSON-only output.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| cli-reference         | agents_default      | CLI controls specialist for Claude Code + OpenAI Codex CLI. Knows slash commands, hooks, flags, sub-agent + MCP integration. Inline cheatsheet for the common case; consults `vault/raw/cli-docs/` for deeper questions. Callable from chat for "how do I do X in CLI Y" questions.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| provider-watcher      | agents_default      | Daily provider/model release watcher. Renders a tight markdown digest of new models, pricing, context-window, and deprecation news for the tracked LLM providers (Anthropic / OpenAI / Google / NVIDIA NIM / ElevenLabs by default). Driven by `tesseract/scheduler/tasks/provider_watch.py` which runs Tavily searches and feeds the agent the results — disabled by default in `schedule.yaml`.                                                                                                                                                                                                                                                                                                                                |
| vision                | vision_agent        | Image-to-text specialist. Describes images, answers questions about them, reads embedded text. Invoked via `invoke_agent` with an `attachment_ids` list when chat_brain is text-only or wants a focused vision pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| design-writer         | agents_default      | From-scratch markdown design-doc author. Produces a single self-contained design document (Context / Architecture / Components / Data Flow / Failure Modes / Open Questions) that the markdown_agent worker writes verbatim to `output_path`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| research-brief        | agents_default      | General research agent. Given a question, runs vault + web searches and returns a tight markdown brief with citations (Question / Findings / Open Threads / Suggested Next Steps). Callable from chat.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| agent-writer          | agents_default      | From-scratch markdown agent author. Given a brief, returns a single self-contained agent `.md` file ready for `agents/provisional/<name>.md` writeback via the `markdown_agent` worker. Operator-approval-gated — generated agents are quarantined under `provisional/` until `agent_promote` moves them to active.                                                                                                                                                                                                                                                                                                                                                                                                              |
| daily-brief           | agents_default      | Daily-brief orchestrator. Invokes the six digester sub-agents (`workspace-digest`, `mission-digest`, `memory-digest`, `vault-digest`, `ecosystem-digest`, `world-digest`) in fixed order and stitches their markdown into a single operator-readable morning brief. Section headers are fixed by `_shared/brief-renderer-spec.md`. Empty digester output → section dropped (voice-friendly). Scheduling + rendering + Mirror surface land in MO-9-8 / MO-9-9.                                                                                                                                                                                                                                                                    |
| workspace-digest      | agents_default      | Workspace activity digester for the daily brief. Reads the prior 24h of workspace events and comments and returns a 3-5 sentence prose paragraph describing what changed in the workspace. Returns empty body when nothing meaningful happened.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| mission-digest        | agents_default      | Yesterday-activity digester for the daily brief. Reports agenda-store items (autonomy + operator work) that went `DONE` or `BLOCKED` inside the window as a short flat bullet list (UPPERCASE prose status labels, no bold), from a renderer-prefetched payload. Mission engine deleted P4 — no longer reads a mission registry.                                                                                                                                                                                                                                                                                                                                                                                                 |
| memory-digest         | agents_default      | Memory-consolidator digester for the daily brief. Reads the overnight dreaming consolidator's "what I learned" notes (not raw memory-store writes) and returns 3-4 sentences of first-person prose. Returns empty body when the consolidator produced nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| vault-digest          | agents_default      | Vault-changes digester for the daily brief. Reads the vault wiki ingest log for new pages and substantive updates over the prior 24h and renders a short flat bullet list (capped at eight entries, plain-prose status labels).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ecosystem-digest      | agents_default      | AI-ecosystem digester for the daily brief (AU-24). Reads the last 7 days of `memory_signal` agenda items, AU-11c discovery memory leaves, AU-11a docs-watch snapshots, and provider_watch daily digests (pre-fetched by `tesseract/orchestrator/brief/ecosystem.py`) and renders 3-5 short prose cards under `## Ecosystem` — narrower than world-digest (AI/agent landscape) and complementary to vault-digest (synthesis, not page listing). Empty all-streams input → section dropped.                                                                                                                                                                                                                                        |
| world-digest          | agents_default      | World-news digester for the daily brief. Receives three fixed pillars (tech / science / politics), pre-fetched Tavily results per pillar, and the operator's interest-affinity profile (`memory-store/interests/profile.yaml`). Emits one structured card per item under per-pillar headings — title / one-paragraph prose summary / `Source:` / `Published:` / raw URL clause. Voice-safe (no inline links, no markdown emphasis). MO-9-13 replaced the MO-9-8 `tracked-topics.yaml` curated-list contract with the pillar+interests substrate.                                                                                                                                                                                 |
| agent-self             | agents_default      | the assistant's autonomous-action default agent. Autonomy kernel routes `WorkerKind.AGENT_SELF` agenda items here when no specific agent is pinned. Full operator-visible tool surface — research (memory / vault / context7 / brave / tavily / pdf), reflect (memory_save / diary_append), build + test (file_write to workshop, bash, delegate_coder / delegate_auditor, invoke_agent), and operator surface (workspace_post / workspace_reply / ask_clarification). Refuses ambiguous goals so the operator can rework instead.                                                                                                                                                                                                          |
| autonomy-strategist   | autonomy_strategist | AU-23 periodic initiative curator. No tool use — receives a pre-fetched substrate payload (agenda outcomes, discovery leaves, vault deltas, worker failures, governor pauses, operator presence) over the lookback window configured in `schedule.yaml::autonomy_strategist` and emits at most three high-conviction `Initiative` objects with slug / goal / rationale / success criteria / evidence / confidence / horizon. Cadence is operator-tunable via the schedule entry's cron expression. Driven by `tesseract/scheduler/tasks/autonomy_strategist.py`; output rides the autonomy bus as `AgendaSource.STRATEGIST`. Operator-attended — every initiative ships with an `operator_review` gate regardless of risk class. |

<!-- audit-loop sub-agents (SU-3.5) -->

| code-auditor | agents_default | Scoped code-audit specialist. Takes a path / target / focus area, runs a deep read-only audit via `delegate_codex_exec`, and emits a structured markdown audit file at `workshop/audits/<YYYY-MM-DD>/audit-<N>.md` using the Critical / Major / Minor / Informational severity grammar that SU-4's `codex_audit` verifier parses. |
| repo-auditor | agents_default | Full-codebase audit specialist. Scans the entire `tesseract/` source tree for a named risk surface. Operator-initiated only — slow, do not run autonomously. Emits a structured markdown audit file using the Critical / Major / Minor / Informational severity grammar. |
| audit-verifier | agents_default | Post-fix re-auditor. Reads a prior audit file as context, re-audits the same scope to confirm fixes landed, and emits a new audit file. If the new audit has no Critical or Major findings, the SU-4 loop terminates. |
| code-fixer | agents_default | Audit-driven fix implementer. Reads a structured audit file, implements fixes for Critical and Major findings via `delegate_coder`, and reports back with commit SHAs and a one-line summary per finding addressed. Minor and Informational findings are ignored unless explicitly requested. Default posture: ask. |
