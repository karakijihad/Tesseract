# Workshop

This folder is the assistant's working space — where task artifacts, drafts, and experiments live.

See `workspace/WORKSHOP.md` for the convention it follows.

Contents here are not version-controlled (gitignored), same as `tesseract/workspace/`.
